server {
    listen 80;

    location / {
        root /usr/share/nginx/html;
        index index.html;
        try_files $uri /index.html;
    }

    location /api/ {
        proxy_pass http://backend:5000/;
        proxy_set_header Host $host;
        porxy_set_header X-Real_IP $remote_addr;
    }

    location /static/ {
        alias /app/staticfiles/;
    }
}